import { styled } from "@mui/material";

export const Container = styled("div")({
    margin: "20vh auto",
    maxWidth: "300px",
    display: "flex",
    flexDirection: "column",
    alignItems: "stretch"
});
