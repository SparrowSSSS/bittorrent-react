import { styled } from "@mui/material";

export const Container = styled("div")({
    display: "flex",
    maxWidth: "400px",
    flexDirection: "column",
    alignItems: "stretch",
    margin: "10vh auto"
});
