import { styled } from "@mui/material";

export const Container = styled("div")({
    display: "flex",
    maxWidth: "400px",
    flexDirection: "column",
    alignItems: "center",
    margin: "10vh auto"
});
