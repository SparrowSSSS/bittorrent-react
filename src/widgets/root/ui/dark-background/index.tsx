import { styled } from "@mui/material";

export const DarkBackground = styled("div")({
    backgroundColor: "#212121",
    width: "100vw",
    height: "100vh",
    position: "absolute",
    left: 0,
    top: 0
});
